// components/ai/InsightCard.tsx — AI insight message with gradient accent

interface InsightCardProps {
  message: string;
}

export default function InsightCard({ message }: InsightCardProps) {
  return (
    <div>
      {/* TODO: AI insight card with gradient accent */}
      <p>{message}</p>
    </div>
  );
}
